export type BankAccount = {
    id: string,
    bankName: string,
    accountName: string,
    accountNumber: number,
    address: string,
    routingNumber: number,
    swiftCode: number,
    currency: string
}